// Define an object named 'Api' to store API endpoint URLs.
const Api = {
  // Define a 'login' property that represents the login endpoint.
  login: "/auth/login",
  quickReport: "/api/alien-report/get-report",
};

// Export the 'Api' object so it can be imported and used in other parts of the application.
export default Api;
